using UnityEngine;
using System.Collections;

public class CanonR_1 : MonoBehaviour
{

    //void OnTriggerEnter(Collider col)
    //{
    //    if (col.gameObject.tag == "Obs_small")
    //    {
    //        col.gameObject.GetComponent<Rigidbody>().isKinematic = false;
    //        col.gameObject.GetComponent<Rigidbody>().AddForce(transform.forward * 3);

    //    }
    //    if (col.gameObject.tag == "Obs_medium")
    //    {
    //        col.gameObject.GetComponent<Rigidbody>().isKinematic = false;
    //        col.gameObject.GetComponent<Rigidbody>().AddForce(transform.forward * 2);
    //    }
    //    if (col.gameObject.tag == "Obs_big")
    //    {
    //        col.gameObject.GetComponent<Rigidbody>().isKinematic = false;
    //        col.gameObject.GetComponent<Rigidbody>().AddForce(transform.forward * 1);
    //    }
    //}
}
